package org.eiredrake.tentacles.repository;

import org.eiredrake.tentacles.model.SchedulingOption;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SchedulingOptionRepository extends JpaRepository<SchedulingOption, Long> {
}