package org.eiredrake.tentacles.repository;

import org.eiredrake.tentacles.model.RelationshipSubject;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RelationshipSubjectRepository
  extends JpaRepository<RelationshipSubject, Long> {}
